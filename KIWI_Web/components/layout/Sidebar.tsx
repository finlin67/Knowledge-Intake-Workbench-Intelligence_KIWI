'use client'

/**
 * Sidebar navigation was replaced by the top navigation bar in {@link TopBar}.
 * This component is kept as a no-op for compatibility; it renders nothing.
 */
export function Sidebar(_props: { expertMode: boolean; onExpertChange: (value: boolean) => void }) {
  return null
}
