"""Inventory table filter modes (GUI + service layer)."""

from __future__ import annotations

# Filter mode keys (combo values use human labels mapped in the GUI).
FILTER_ALL = "all"
FILTER_REVIEW_REQUIRED = "review_required"
FILTER_FAILED = "failed"
FILTER_WORKSPACE = "workspace"
FILTER_MATCHED_BY = "matched_by"
